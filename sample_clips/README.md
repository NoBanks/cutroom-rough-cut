# Sample clips

Drop sample `.mp4` or `.mov` footage into this directory. CUTROOM will list the
files here when someone clicks **Run the sample footage**.

The first milestone accepts up to 10 sample clips with a combined size of
200MB.